class Node:
    def __init__(node):
        left = None
        right = None

def rotate(root):
    #Do BFS
    leftSide = node.left
    rightSide = node.right

    node = root
    while node:
        if node.right:

            node = node.right
        if node.left:
            node.pop()
            node = node.left
